apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: kgateway-mcp
  namespace: kgateway-system
spec:
  gatewayClassName: kgateway-mcp
  listeners:
  - protocol: kgateway.dev/mcp
    port: 8080
    name: http
    allowedRoutes:
      namespaces:
        from: All

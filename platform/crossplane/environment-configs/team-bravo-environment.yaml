apiVersion: apiextensions.crossplane.io/v1beta1
kind: EnvironmentConfig
metadata:
  name: team-bravo-environment
  labels:
    team: team-bravo
data:
  # Team-specific repository defaults
  team: "team-bravo"
  requiredApprovingReviewCount: 8
  defaultWebhookUrl: "https://bravo-team.company.com/webhook"
  serviceTier: "critical"
  #requireSignedCommits: "true"

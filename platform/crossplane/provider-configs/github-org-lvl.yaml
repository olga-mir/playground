apiVersion: github.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: github-provider-org-lvl
spec:
  credentials:
    source: Secret
    secretRef:
      name: github-provider-credentials-org
      namespace: crossplane-system
      key: credentials

project: ai
syncWave: "1"

name: kgateway-ai
chart: kgateway
repoURL: cr.kgateway.dev/kgateway-dev/charts
targetRevision: v2.1.0-main
namespace: kgateway-system
helm:
  values:
    agentGateway:
      enabled: true
    discoveryNamespaceSelectors:
    - matchLabels:
        kubernetes.io/metadata.name: ai-gateway-config


    #gateway:
    #  aiExtension:
    #    enabled: true
        # I don't understand agentGateway vs aiExtention
        # https://kgateway.dev/docs/ai/setup/
        # inferenceExtension

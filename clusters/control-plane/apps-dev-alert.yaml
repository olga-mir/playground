---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: github-dispatch-apps-dev
  namespace: flux-system
spec:
  type: githubdispatch
  address: https://github.com/olga-mir/playground
  secretRef:
    name: github-webhook-token
---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Alert
metadata:
  name: github-dispatch-apps-dev-cluster
  namespace: flux-system
spec:
  providerRef:
    name: github-dispatch-apps-dev
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: apps-dev-cluster
      namespace: flux-system
  eventMetadata:
    project: "${PROJECT_ID}"
    cluster: "apps-dev"
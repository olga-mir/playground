apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - providers/
  - functions/
  - compositions/
  # - rbac/ TODO - is it required? if yes why doesn't it work without
  - namespaces/

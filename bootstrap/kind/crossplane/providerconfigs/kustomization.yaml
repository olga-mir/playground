apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - providers-config.yaml
  # TODO this is kubeconfig secret needed for k8s provider (maybe not even needed - validate later)
  #- providerconfig-gke-control-plane.yaml

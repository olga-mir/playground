apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: gke-control-plane-cluster-k8s-config
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: ${GKE_CONTROL_PLANE_CLUSTER}-kubeconfig
      key: kubeconfig

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Control-plane cluster overlay - full crossplane + platform services
resources:
  # Apply GKE Cluster Custom Resource
  - ../../base/gkecluster-apps-dev

  # Install components (TODO - need to move to fluxtomizations and add dependency)
  #- ../../base/crossplane-system
  - ../../base/platform-services
  - ../../base/kagent
  - ../../base/kagent-system
  - ../../base/kgateway-system

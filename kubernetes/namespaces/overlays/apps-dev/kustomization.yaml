apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apps-dev cluster overlay - minimal setup for workload cluster
resources:
  # Add tenant workloads and basic platform services as needed
  - ../../base/kagent
  - ../../base/kgateway-system
  - ../../base/litmus

---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: litmus-core
  namespace: litmus
spec:
  interval: 10m
  chart:
    spec:
      chart: litmus-core
      version: "3.23.1"
      sourceRef:
        kind: HelmRepository
        name: litmuschaos
        namespace: litmus
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    # Litmus core installs ChaosEngine CRDs and chaos operator
    # Minimal configuration - uses defaults

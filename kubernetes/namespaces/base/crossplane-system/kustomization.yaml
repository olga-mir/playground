apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Full crossplane system - base components (excluding providers and providerconfigs which are handled separately)
resources:
  - install
  - environment-configs
  - functions
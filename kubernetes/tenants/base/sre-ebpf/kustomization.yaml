apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# SRE team - ebpf-noisy-neighbour workload (https://github.com/olga-mir/playground-sre)
# Flux watches the same source repo as the sre tenant (playground-sre GitRepository).

resources:
  - namespace.yaml
  - flux-kustomization.yaml
  - image-repository.yaml
  - image-policy.yaml
  - image-update-automation.yaml

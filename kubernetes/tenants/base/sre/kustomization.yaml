apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# SRE team - perf-lab service (https://github.com/olga-mir/playground-sre)
# Flux watches the source repo directly; image promotion is automated via ImageUpdateAutomation.

resources:
  - namespace.yaml
  - gitrepository.yaml
  - flux-kustomization.yaml
  - image-repository.yaml
  - image-policy.yaml
  - image-update-automation.yaml
